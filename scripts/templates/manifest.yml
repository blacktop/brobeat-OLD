module_version: 1.0

vars:
  paths:
    default:
      - /var/log/bro/{{ field.log_type }}.log*
  pipeline:
    # options: with_plugins, no_plugins, json_with_plugins, json_no_plugins
    default: with_plugins

ingest_pipeline: ingest/{{ '{{pipeline}}' }}.json
prospectors:
  - config/bro-{{ field.log_type }}.yml